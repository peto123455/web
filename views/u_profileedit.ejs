<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('_links') %>
    <title>SPM - User edit</title>
</head>
<body>
<%- include('_navbarLogged') %>
<div class="container pt-5" style="max-width: 500px;">
    <h1 class="pb-4"><%= user.username %></h1>
    <form action="" method="POST">
        <div class="form-group">
					<div class="form-group">
            <label for="email">Email</label>
            <input type="email" id="email" name="email" class="form-control" value="<%= user.email %>" required>
        	</div>
					<label for="avatar">Avatar</label>
          <select id="avatar" name="avatar" class="form-select form-control" aria-label=".form-select-sm example">
            <% for (let i = 1; i <= 7; i++) { %>
                <option value="<%= i %>" <% if(i == user.avatar) { %> selected <% } %> ><%= i %></option>
            <% } %>
          </select>
        </div>
        <input type="submit" class="btn btn-success float-right" value="Save">
        <a href="/profile?id=<%= user._id %>" class="btn btn-primary float-right mr-2">Cancel</a>
    </form>
</div>
</body>
</html>